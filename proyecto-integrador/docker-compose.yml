version: '3.8'

services:
  redis:
    image: redis:alpine

  worker:
    build: ./worker
    depends_on:
      - redis

  web:
    build: ./web
    ports:
      - "5000:5000"
    depends_on:
      - redis
