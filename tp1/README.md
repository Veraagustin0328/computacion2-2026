Trabajo Practico N1
Computacion II
Agustin Vera


Monitor de Procesos y Threads (Computación II - 2026)
Este proyecto es un monitor interactivo en tiempo real para Linux (estilo htop), enfocado en mostrar la anatomía interna de los procesos y sus hilos. La información se extrae leyendo directamente del sistema de archivos /proc, sin usar librerías de alto nivel como psutil.
El proyecto fue desarrollado y probado localmente en un entorno Linux (Ubuntu sobre WSL2) y está completamente contenerizado en Docker para su entrega y ejecución.
El sistema está diseñado como una arquitectura multiproceso distribuida: un recolector central busca los procesos activos, 7 analizadores en paralelo extraen métricas específicas, un agregador centraliza el snapshot en memoria compartida y la interfaz TUI (hecha con rich) permite navegar por los datos en tiempo real1. Cómo correr el proyectoEl proyecto está preparado para ejecutarse dentro de un entorno Docker. Bash# Clonar el repogit clone <URL_DEL_REPO>cd <NOMBRE_DEL_REPO> # Levantar con docker-compose (interactivo)docker compose up --buildAtajos de teclado en la TUI1 a 7 o r / m / f / t / s / p / g: Cambiar de vista (Resumen, Memoria, FDs, Threads, Señales, Scheduling, Sistema). Flechas arriba/abajo: Navegar por la lista de procesos. Enter: Fijar (pin) un proceso en el panel inferior para seguirlo aunque cambie el orden de la lista. / / u: Filtrar por comando o por usuario. c: Alternar el criterio de ordenamiento (CPU%, RSS, PID). + / -: Aumentar o reducir el intervalo de refresco de la vista actual. q: Salir de forma limpia. 2. Arquitectura del SistemaPara organizar el trabajo y evitar cuellos de botella en la recolección, dividimos la lógica en componentes independientes que se comunican entre sí:                   ┌─────────────────────────────────────────┐                  │             SNAPSHOT GLOBAL             │                  │        (multiprocessing.Manager)        │                  │  ┌───────────────────────────────────┐  │                  │  │ "resumen", "memoria", "fds", ...  │  │                  │  └───────────────────────────────────┘  │                  └─────────▲─────────────────────▲─────────┘                            │ escriben            │ lee ┌──────────────────────────┼─────────┬───────────┴──────────┐ │                          │         │                      │┌┴─────────┐ ┌─────────┐ ┌──┴───┐  ...┌┴──────────┐   ┌──────┴─────┐│Recolector│ │Resumen  │ │Memoria│    │Sistema    │   │ Display    ││(pids)    │ │cada 2s  │ │cada 3s│    │cada 2s    │   │ TUI        │└────┬─────┘ └────▲────┘ └──▲───┘    └────▲──────┘   │ (rich)     │     │ Queue      │         │             │           └────────────┘     └────────────┴─────────┴─────────────┘                Distribución de PIDsMódulos principales:main.py: Punto de entrada. Inicializa la memoria compartida, arranca los procesos hijos y gestiona el apagado limpio al recibir señales. procfs.py: Módulo auxiliar donde centralizamos todas las funciones para leer y parsear los archivos de /proc (stat, status, cmdline, maps, etc.). recolector.py: Proceso que lista los PIDs en /proc y los reparte a los analizadores mediante una Queue. analizadores/: 7 procesos independientes que corren en bucle, cada uno a su propio ritmo. Leen sus datos específicos de /proc y guardan el resultado en el diccionario compartido. display.py: La interfaz de usuario que consulta el snapshot global y refresca la pantalla según la vista seleccionada. senales.py: Lógica para reaccionar a las señales que recibe el monitor (SIGINT, SIGUSR1, SIGHUP, etc.). 3. Decisiones de Diseño e IPC